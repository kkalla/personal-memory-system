# T4 scope/status 재검증 실행 계획

2026-09-14, 기준 e9eb736. 사용자가 직전 검증 기록의 다음 수정안을 승인하고 진행을 요청했다.

- T2 S1/S2/S6과 대조한 scope/status 결정표로 호출자 지침을 정리한다. metadata API/제품 저장 계약은 변경하지 않는다.
- 기존 CLI 훅 경계에서 프로젝트 식별 진행·실패 시 이전 검색 사용, 재검색 scope/필터 변경을 거부하는 RED→GREEN을 추가한다.
- 기존 집중 10세션·12턴을 같은 fixture/hash/프로젝트 매핑으로 한 번 실행한다. 명시적 전역 확인 합성 사례를 두 모드에 추가해 global/confirmed가 잘못 격리되지 않는지 검증한다. 이 2세션·2턴은 기존 11개 fixture와 별도 집계한다.
- 집중 10건 행동 및 연결과 전역 보충 2건이 모두 통과하고 복원 3건의 부재/충돌 검증이 통과할 때만 전체 44세션·52턴을 수행한다. 집중 검증 실패 시 전체 회귀는 미실행으로 남기고 실패를 보고한다.
- 새 실행 상한은 **56세션·66사용자 턴**(12/14 집중·보충 + 조건부 44/52 전체)이다. 이전 실제 훅 프로브는 재사용한다. 같은 구성의 성공할 때까지 재시도는 하지 않는다. 인증/한도/기록 장애는 첫 장애에서 이후 실행을 중단한다.
- 모델은 Claude claude-sonnet-5/high, Codex gpt-6-astra/low를 유지한다. 기존 호출당 시간/Claude 예산 제한을 유지한다.
- 최종 전체 실행 시 보관 12건의 부재·충돌 복원 검증을 분리하여 기록한다. 부모 부재 합성 검증과 실제 archive 경로 조건을 혼동하지 않는다.
- 실제 볼트·전역 CLI 설정·launchd·사용자 Stop hook·설치는 변경하지 않는다. 원시 모델 자료는 로컬 미추적, 코드·테스트·집계만 커밋한다.

## 실행 결과

집중 10건 행동/9건 연결, 전역 보충 2건 행동 모두 pass. 복원 명령 충돌 보존은 MR-001 실패(3건 중 2건 통과)로 전체 회귀 진입 조건 미달이다. 실제 12세션·14턴 실행 후 중단했으며 전체 44세션은 미실행이다. [검증 기록](../../memory-retrieval-t4-scope-status-validation.md)에 근거와 다음 수정 대상을 남겼다.
