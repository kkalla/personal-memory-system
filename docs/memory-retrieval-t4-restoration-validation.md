# T4 복원 안내 및 전체 회귀 — 2026-09-14

기준 898a4aa. [실행 계획](superpowers/plans/2026-09-14-memory-retrieval-t4-restoration-plan.md)에 따라 보관 집중 후 전체 회귀를 실행했다. 최종 집계는 아래에 기록한다.

## 변경과 검증 방식

주의 문구만으로 일반 mv를 보완하지 않고, 복사해 실행할 코드 블록 자체에 목적지 존재 검사와 mv -n을 함께 넣도록 공통 지침을 구체화했다. 기존 scope/status 결정표·검색 지침·훅·제품 저장 API·핵심·fixture는 유지했다.

한 집중 응답이 원래 작업공간의 절대 경로를 제시했다. 별도 복제본 검증이 원본에 쓰지 않도록 검증기에 opt-in 경로 치환을 추가했다. 원래 argv를 보존하고 원래 workspace 절대 경로 접두사만 복제본 접두사로 치환한다. 명령의 조건·플래그·파일 상대 위치는 바꾸지 않는다. 이 검증은 명령의 이식성을 증명하는 것이 아니라 동일한 작업공간 구조의 부재/충돌 동작을 확인한다. 임의 셸 명령을 가두는 샌드박스가 아니므로 각 코드 블록은 실행 전에 사람이 검토했다.

절대 경로가 원본을 변경하지 않는 테스트를 RED→GREEN으로 추가했다. 모델에 전달되는 구성은 집중·전체 캠페인 중 변경하지 않았다. 검증 보조 코드만 집중 캠페인 중 확장했으므로 집중의 초기 configuration과 최종 verification-configuration을 모두 보존하고 restore-check마다 검증기 hash를 기록한다.

로컬 테스트 43개 PASS, 기존 hooks 두 그룹 PASS, 임시 memory_mcp selftest OK, fixture 구조 11건 PASS, git diff --check PASS. 구조/로컬 검증은 모델 행동 성공과 다르다.

## 집중 회귀

Claude MR-001/007/009 × 두 모드의 6세션·8턴을 실행했다. 행동 6/6, 회상 연결 3/3, 제공 모드 연결은 해당 없음. 실제 복원 코드 6개에 대해 부재·충돌 복제본 **12조건 모두 통과**했다. 원시 workspace는 유지했다. 이에 따라 같은 모델 입력 구성으로 전체 회귀에 진입했다.

근거: [집중 판정](evaluations/memory-retrieval-t4-restore-focused-20260914/records.json). 이전 일반 mv 실패는 [이전 기록](memory-retrieval-t4-scope-status-validation.md)에 보존한다.

## 전체 회귀

기존 fixture SHA `48cf67fb1cde05ba4e4ad7fe0d542d6b4959bf13109989462c10e3c30054881d`와 같은 등록 프로젝트 매핑을 사용한다. Claude는 claude-sonnet-5/high, Codex는 gpt-6-astra/low다. Codex CLI 이벤트가 실제 응답 모델 버전을 보고하지 않은 경우 unknown을 유지하고 요청 모델과 구분한다.

총 **44세션·52사용자 턴**이 실행 완료됐다. 재시도는 없으며 집중과 합쳐 승인된 상한인 50세션·60턴을 사용했다.

| 환경·모드 | 행동 | 실제 회상 연결 |
| --- | --- | --- |
| Claude provided-memory | 11/11 | 해당 없음 |
| Claude recall-integration | 11/11 | 10/11 |
| Codex provided-memory | 11/11 | 해당 없음 |
| Codex recall-integration | 11/11 | 11/11 |
| 합계 | **44/44** | **21/22** |

MR-003 네 실행은 프로젝트 직접 정정을 project/confirmed + scope_inferred=true로 저장하고 범위/상태의 이유를 분리했다. MR-011 네 실행은 프로젝트 문맥 없는 정정을 null/candidate로 보관했다. 후보의 자동 적용, 다른 프로젝트 규칙 유입, 일회성 예외에 의한 영구 선호 덮어쓰기는 관찰되지 않았다. MR-007/008은 같은 세션에서 두 턴을 실행해 예외 종료와 프로젝트·주제 변경을 확인했다.

보관 MR-001/007/009 × 두 환경 × 두 모드의 실제 복원 명령 12개를 별도 복제본에서 검증했다. 목적지 부재 **12/12**, 목적지 충돌 **12/12** 통과했다. 합성 로컬 테스트의 부모 디렉터리 부재 조건을 이 24조건과 혼동하지 않는다. 이 추가 검증은 fixture 행동 점수와 별도로 집계했다.

[전체 기준별 판정](evaluations/memory-retrieval-t4-restore-final-20260914/records.json), [전체 집계](evaluations/memory-retrieval-t4-restore-final-20260914/summary.json). 모델 답변·실제 파일/기억 변경·MCP 검색 추적을 직접 검토했다. 기록 생성기는 수동 판정을 집계하며 의미 판정을 자동 수행하지 않는다. 개별 result.json의 원시 not_run 필드는 보존하고 검토 결과를 records.json에 분리했다.

## 남은 실패와 후속 작업

**Claude MR-002 recall-integration은 연결 실패다.** 프로젝트 ID는 올바르게 식별했지만 검색어 `서비스이름`, `중복검증`, `네이밍` 모두 결과가 비었다. 필요한 프로젝트 워크플로우 이름 정책을 실제로 반환받지 못했다. 최종 답변의 프로젝트별·대소문자 무시 중복 판단은 맞았으므로 행동은 pass이며, 이를 기억 회상 성공으로 해석하지 않는다.

다음 수정 후보는 키워드 검색의 0건 응답 이후 재검색 방식이다. 이번 검색 실패를 고정 사례로 보존하고, 띄어쓰기·짧은 핵심어 분해 또는 같은 프로젝트 안의 제한된 대체 검색 중 최소 변경을 검토한다. 현재 근거만으로 의미 검색을 도입하거나 확정 설계를 변경하지 않는다. 다음 변경 시 실패 입력을 먼저 재현하고 새 구성의 집중 검증과 전체 회귀를 별도 실행해야 한다. 이번 결과를 22/22로 표기하거나 성공할 때까지 재실행하지 않았다.

한 번의 행동 통과는 지속적인 모델 준수를 보장하지 않으며, 실제 환경 설치와 운영 연결은 별도 후속 작업이다. 기존 fixture와 실행기는 계속 회귀에 재사용하되 매번 구성 hash·실행 여부·행동·연결을 함께 기록한다. 과거 44/44·22/22 기록을 현재 구성의 결과로 재사용하지 않는다.

집중 82개·전체 453개 근거 파일은 scrub 검사 0건 확인 후 로컬 docs/evaluations에 보존했다. 원시 workspace/vault·인증 홈·인증 런처는 복사하지 않았다. 원시 모델 근거는 미추적으로 유지하며 코드·테스트·집계 문서만 커밋한다. 실제 볼트·전역 CLI 설정·launchd·기존 사용자 Stop hook·설치는 변경하지 않았다.
