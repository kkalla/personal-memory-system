# T2 최초 핵심 기억 선정 기록

상태: **2026-09-11 사용자 승인으로 K1–K4 확정. 실제 볼트에는 저장하지 않음.**
기준: 설계 `ee8b367`, T1 `d079f04`. 검토일: 2026-09-10.

## 검토 범위와 선정 기준

실제 볼트 `/Users/max/99_memory/memory`의 유형별 노트 102개를 목록·description으로 검토하고, 아래 후보와 주요 제외 항목은 본문을 읽었다. 모든 노트 전문을 검증한 전수 내용 감사는 아니다. 인덱스와 백업 인덱스는 기억 후보에서 제외했다. 102개 노트에는 `scope`, `status` 필드가 없었다. 따라서 파일 유형이나 단정적인 문체를 기존 전역 확정 메타데이터로 취급하지 않았다.

선정 기준은 여러 프로젝트에 재사용되는 사용자 선호 또는 반복 정정, 잘못 적용했을 때의 비용, 짧게 써도 조건을 보존할 수 있는지다. K1–K4는 검토 당시 핵심 선정 후보였으며 저장의 `status=candidate`와는 별개다. 2026-09-11 사용자 확인으로 아래 축약 본문의 전역 적용과 최초 핵심 선정을 함께 확정했다. 원본 전체를 전역 규칙으로 승격하지 않는다.

## 승인된 정확한 본문

다음 블록 전체가 승인된 최초 핵심 제공 본문이다. 제목·불릿·마지막 LF도 예산에 포함한다. 출처 설명과 검토 이유는 주입하지 않는다.

<!-- core-body:start -->
```text
# 핵심 기억
- git 미추적 개인 설정·파일을 정리할 때는 삭제보다 archive/ 이동을 기본으로 제안하고 복원 방법을 남긴다.
- 내가 만들지 않은 워킹트리 변경은 보존한다. 원인을 단정해 되돌리지 않는다. 다른 ref 조회는 diff/log/show로 하고, 미커밋 변경이 있는 뮤테이션 테스트의 원복은 작업 전 파일 백업으로 한다.
- 계획서의 테스트와 완료 보고를 실행 검증으로 간주하지 않는다. 테스트 실효성이 의심되면 결함을 넣었을 때 실제로 실패하는지 확인한다.
- 인증 정보를 출력해 확인하지 않는다. 토큰 유효성은 허용된 인증 요청의 성공·실패로 확인하고, 환경·설정 출력에 비밀값이 섞이지 않게 한다.
```
<!-- core-body:end -->

이 본문에도 확정 설계의 충돌 우선순위가 적용된다. 현재 지시의 일회성 예외로 원본을 영구 변경하지 않는다. 인증 요청 문장은 새 네트워크 작업 권한을 부여하지 않는다.

로컬 계수: 위 블록은 마지막 LF 포함 UTF-8 **799바이트**다. 확정 예산 2048 대비 1249가 남는다. 실제 모델 토큰 수를 측정한 값은 아니다.

| ID | 출처 basename | 선정 이유와 축약 경계 |
| --- | --- | --- |
| K1 | `feedback_archive-over-delete-personal-config.md` | 명시적 보관 선호. 정리 요청에서 재설명 없이 복구 가능성을 보존한다. 모든 파일 삭제를 금지하는 규칙으로 넓히지 않았다. |
| K2 | `feedback_repo-claude-md-symlink-conversion.md`, `feedback_git-checkout-branch-dashdash-dot-footgun.md` | 사용자 동시 편집을 되돌린 정정과 서로 다른 프로젝트의 미커밋 작업 손실 사례. 프로젝트별 CLAUDE.md 구성은 제외하고 작업 보존만 묶었다. 이 일반화는 2026-09-11 사용자 확인을 받았다. |
| K3 | `feedback_plan-authored-tests-vacuous.md`, `feedback_mr-review-response-verification.md` | 계획·자기 보고를 검증으로 오인하는 반복 실패를 예방한다. 모든 작업에 뮤테이션 테스트를 강제하지 않고 실효성이 의심될 때로 제한했다. |
| K4 | `feedback_env-token-debugging.md` | 여러 프로젝트에서 반복된 비밀값 출력 사고의 공통 정정. 특정 CLI 훅의 차단 패턴·우회법·토큰 공유 지침은 포함하지 않았다. |

## 핵심에서 제외하고 검색에 남길 항목

제외는 삭제가 아니다. 원본과 전체 목록은 보존한다. 레거시 노트의 실제 scope/status 지정은 [저장 계약](memory-retrieval-storage-contract.md)에 따른 별도 검토다.

| 항목 | 판단과 검색 시점 |
| --- | --- |
| `feedback_gitlab-review-tone-customization.md` | 공식 존댓말은 리뷰 봇 출력의 프로젝트 요구다. 일반 대화 말투로 전역화하지 않는다. 해당 봇 프롬프트·리뷰 스타일 작업 때 검색. |
| `user_tech-stack-python-typescript.md` | 당시 확장자 스캔 관찰이다. 언어 금지나 새 언어에 대한 숙련도 단정은 승인된 선호가 아니다. 스택 선택 때 참고 자료로 조회하고 현재 코드·요청을 우선한다. |
| `feedback_196-server-compose-project-per-repo.md` | 관리포인트 축소 선호는 유력하지만 스택·자격 증명·서비스 격리 조건과 묶여 있다. 배포·서비스 신설 때 검색. “항상 통합/토큰 공유”로 축약하지 않는다. |
| `feedback_claude-p-agent-over-keyword-search.md` | 코드 진단 방식과 특정 실행 환경에 관련된 선호다. 코드 조사·에이전트 실행 설계 때 검색. 모든 작업의 CLI 선택으로 확대하지 않는다. 이 항목은 description 수준 검토다. |
| `feedback_icloud-write-contention-hallucination.md`, `feedback_customer-doc-code-parity-verification.md` | 진단 근거·문서 검증은 중요하지만 적용 절차가 길고 상황별이다. 파일 충돌 진단·대외 변경 문서 검토 때 원문 검색. |
| `feedback_transcript-write-guardrail.md` | 환경별 제약 기록이다. 대화 기록 처리 작업 때 검색·현 환경 확인. 항상 제공할 개인 선호로 취급하지 않는다. |
| `project_agentic-ocr-workflow-name-uniqueness.md` 및 나머지 `project_*` | 프로젝트 요구·진행·운영 상태다. 관련 프로젝트·주제에서 검색하며 전역 핵심에서 제외한다. 오래된 “다음 T1”은 현재 작업 단계의 근거로 사용하지 않는다. |
| 나머지 `feedback_*`, `reference_*` | 도구 사용법, 외부 참조, 특정 장애·배포 절차는 작업 관련 검색에 남긴다. 이번 선정에서 빠졌다는 이유만으로 candidate나 폐기 상태로 쓰지 않는다. |

## 확인 기록

- 확인 날짜: 2026-09-11 (Asia/Seoul).
- 확인 요청: “문서의 K1–K4 본문과 S1–S6 계약안을 그대로 확정할까요?”
- 사용자 응답: `go`.
- 확정 범위: K1–K4 위 본문 그대로 최초 전역 확정 핵심으로 선정하고, 저장 계약 S1–S6을 채택한다. 본문 수정·계약 예외는 없다.
- 예산도 S5 그대로 `utf8-byte-v1`, 2048바이트 대리 계산을 채택한다. 실제 모델 토큰 수를 뜻하지 않는다.
- T2 완료: 후보 제시·이유 및 제외 판단·저장 계약 구체화·사용자 확인 기록 완료.
- 실제 볼트 쓰기와 T3/T4 실행은 이번 승인에 따른 T2 작업 범위 밖이다. T3의 `approval_ref`는 이 문서의 확인 기록을 가리킨다.
