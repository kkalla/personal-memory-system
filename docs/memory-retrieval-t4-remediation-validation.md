# T4 보강 및 Sonnet 5 high 회귀 기록 — 2026-09-14

상태: 승인된 보강 구현과 65세션·76사용자 턴의 검증을 완료했다. 최종 행동 43/44, 회상 연결 18/22 pass로 전체 성공 목표는 미달이다. 실제 볼트·전역 CLI 설정·launchd·기존 사용자 Stop hook은 변경하지 않았다.

## 기준과 변경

사용자가 Claude 기본값을 Sonnet 5 high로 지정하고 보강 계획 진행을 승인했다. CLI 명시 모델은 `claude-sonnet-5`, effort는 `high`이며 실제 응답 모델도 `claude-sonnet-5`로 기록한다. Codex는 기존 `gpt-6-astra`, low다. low 결과는 [기존 검증 기록](memory-retrieval-t4-validation.md)에 보존하며 모델 전체의 우열이나 안정적 성공률로 일반화하지 않는다.

- **P1:** 정적 적용/출력 지침과 검색 절차를 분리하고 시스템/개발자 지침으로 전달했다. SessionStart는 검증된 핵심만 제공한다. 제공 기억 모드에는 검색 절차/검색 도구/핵심 훅을 주입하지 않는다.
- **P2:** Claude memory_search의 PreToolUse에서 공백 구분 단어 하나만 허용한다. 빈 쿼리·여러 단어는 내부 사유로 거부하고 범위/검색어를 임의로 바꾸지 않는다. 사용자 설명이나 파일 작업은 가로채지 않는다. 제품 memory_search의 AND 검색·필터 계약은 유지한다.
- P2 비교의 PostToolUse 시도는 실제 13회 모두 `{}`였고 미적중 피드백 전달을 확인하지 못했다. 최종 구성에서 제거했다. 개선 원인을 이 후처리 훅에 돌리지 않는다.
- **P3:** 계획 권장안에 대한 진행 승인을 근거로 K1/사전 기억의 archive 기준을 대상 설정 디렉터리 내부로 명시했다. [승인 근거와 정확한 본문](memory-retrieval-core-review.md)에 최초 문구도 보존했다. 현재 핵심은 861/2048 UTF-8 바이트이며 실제 모델 토큰 수가 아니다.
- 평가 스냅샷에 파일 내용뿐 아니라 디렉터리 목록을 추가했다. 복원 안내는 원시 workspace와 분리한 복제본에서 검토한 실제 명령으로 검증한다.
- 결과 레코드에 effort 및 지침·훅·핵심 소스 hash를 추가했다. 각 캠페인의 실제 구성 파일을 별도 보존한다.

## 같은 입력으로 수행한 단계별 비교

대상: Claude MR-001/002/003/006/008/010/011의 recall-integration, 구성당 7세션·8사용자 턴. 모두 최초 fixture SHA `4a6b3e727f72374e67fda50acaf9b203f612ab750e663e4f2ec1ba59e914c908`과 같은 프로젝트 ID 매핑을 사용했다. 기대/금지 행동은 모델에게 주입하지 않았다.

| 구성 | 행동 pass | fail | inconclusive | 연결 pass | fail |
| --- | ---: | ---: | ---: | ---: | ---: |
| 기존 지침, Sonnet 5 high | 2 | 4 | 1 | 2 | 5 |
| P1 정적 지침 보강 | 6 | 1 | 0 | 4 | 3 |
| P2 검색어 검사 추가 | 7 | 0 | 0 | 6 | 1 |

각 구성은 한 번씩 실행했다. 지침 위치·내용을 P1에서 함께 바꿨으므로 둘의 개별 효과를 분리한 실험은 아니다. 구성당 작은 표본이며 반복 안정성을 입증하지 않는다.

- high 기준에서도 긴 AND 쿼리와 출력 메타 문구가 남았고, MR-001은 파일 내용 keep-me를 작업 중단 지시처럼 읽어 보관을 수행하지 않았다. effort만으로 해결되지 않았다.
- P1은 MR-001 보관/복원, MR-006 두 문장, MR-008 전환 후 완성 리뷰를 충족했다. MR-010은 관련 기억을 놓치고 영어로 답해 합니다/입니다체 기준을 위반했다.
- P2는 MR-002/010에서 단일 핵심어를 통해 실제 기억을 반환받았다. MR-001은 파일명 위주 검색만 남아 연결 fail이나 보관 행동은 pass였다.
- 후보/문맥 없는 저장 사례는 실제 metadata와 파일 변경으로 확인했다. 연결 pass는 검색이 필요한 프로젝트/문맥에서 실제 호출된 근거를 별도로 평가했다. 빈 사전 저장소는 관련 노트 미반환이 정상이다.

근거: [high 기준](evaluations/memory-retrieval-t4-high-baseline-20260914/records.json), [P1](evaluations/memory-retrieval-t4-high-p1-20260914/records.json), [P2](evaluations/memory-retrieval-t4-high-p2-20260914/records.json). 원시 결과는 손대지 않고 수동 판정은 별도 records.json에 기록했다. 근거는 로컬 미추적 자료다.

## 최종 전체 회귀의 비교 경계

새 fixture SHA: `48cf67fb1cde05ba4e4ad7fe0d542d6b4959bf13109989462c10e3c30054881d`. MR-001/007/009의 사전 기억과 adaptation에 보관 경로 기준을 명시했으며 기존 기대/금지 행동과 ID는 유지했다. [개정 기록](memory-retrieval-evaluation-contract.md)에 전후 hash를 남겼다. 입력을 명확히 한 최종 캠페인을 최초 입력과 같은 실험이라고 하지 않는다.

두 환경은 같은 매핑/새 fixture를 사용하고 각각 순차 실행한다. 한 환경의 실행 장애가 생기면 다른 환경도 현재 진행 건 완료 후 다음 실행을 중단한다. 행동 실패는 기록하며 남은 케이스를 정상 실행한다. 코드/입력은 캠페인 중 변경하지 않는다.

## 최종 전체 회귀 결과

44세션·52사용자 턴 모두 실행 완료했다. 실행 오류·한도 중단·inconclusive는 없었다. 수동 판정은 원시 result의 not_run 값을 덮어쓰지 않고 [records.json](evaluations/memory-retrieval-t4-final-20260914/records.json)에 분리했다. [집계](evaluations/memory-retrieval-t4-final-20260914/summary.json)와 각 configuration 소스로 실행 구성을 추적할 수 있다. 최종 44건 모두 fixture·실행기·지침·훅·핵심 hash가 동결한 소스와 일치함을 확인했다.

| 환경 / 모드 | 행동 pass | fail | 회상 연결 pass | fail |
| --- | ---: | ---: | ---: | ---: |
| Codex / 제공 기억 | 11 | 0 | 해당 없음 | 해당 없음 |
| Codex / 실제 회상 | 11 | 0 | 11 | 0 |
| Claude / 제공 기억 | 10 | 1 | 해당 없음 | 해당 없음 |
| Claude / 실제 회상 | 11 | 0 | 7 | 4 |

남은 실패는 다음과 같다.

- **Claude MR-011 제공 기억 — 행동 fail:** 프로젝트 문맥이 없는데 실제로 global/confirmed를 저장했다. 기대 행동 2(범위 미확인 후보 보관)와 금지 행동 1(전역 confirmed 저장 금지)을 위반했다. 현재 지침에도 격리 규칙은 전달되어 있으므로 지침 부재로 설명하지 않는다.
- **Claude MR-001/007/009 실제 회상 — 연결 fail:** 보관 행동은 맞았으나 memory_search를 호출하지 않았다. MR-007은 두 턴 모두 생략했다. 핵심 본문 제공과 올바른 결과만으로 검색 연결을 통과시키지 않았다.
- **Claude MR-002 실제 회상 — 연결 fail:** 검색은 했으나 `중복검증`, `invoice`, `대소문자`가 모두 미적중했다. 올바른 답변을 관련 기억 반환의 증거로 대신하지 않았다.

Claude MR-011 실제 회상은 첫 scope=null/status=confirmed 저장이 계약 검증에서 거부되고, 재시도에서 null/candidate로 실제 보관되었다. 최종 상태 기준 pass이며 첫 시도부터 준수했다고 주장하지 않는다. Codex의 빈 저장소 MR-011 memory_get 미존재 오류도 실행 전체 장애로 세지 않았다.

MR-001/007/009 × 두 환경 × 두 모드의 복원 명령 **12건**을 검토 후 별도 workspace 복제본에서 실행했다. 원래 경로와 내용 복원, 원시 workspace 불변을 확인하고 각 restore-check.json에 기록했다. MR-001 활성 파일과 MR-007 일회성 삭제 뒤 보존 대상도 스냅샷으로 확인했다. 이 검증은 fixture의 목적지 부재 조건이다. Claude의 일반 mv 안내가 복원 목적지 충돌에서도 덮어쓰기를 막는다는 보장은 없으며, 충돌 안전성 전체를 검증한 결과로 확대하지 않는다.

단일어 PreToolUse는 호출된 쿼리 모양만 검사한다. 검색 자체의 생략과 잘못된 핵심어 선택은 막지 못했다. 다음 보강에서 검토할 구체 과제는 요청/작업 전환별 실제 검색 수행 확인, 미적중 핵심어 선택, 문맥 없는 저장의 전역 확대 방지다. 이번 실행 상한 안에서 실패를 재시도해 통과로 바꾸지 않았다. 실사용 환경 설치 및 전체 성공 선언은 하지 않았다.

새 캠페인 4개의 근거 481개 파일을 기존 scrub_secrets로 검사해 패턴 적중 0건을 확인한 뒤 로컬 docs/evaluations에 복사했다. 인증 홈·토큰은 복사하지 않았다. 이 자료는 이전 자동 승인 검토에서 원시 모델/기억 자료의 git 포함을 거부한 경계를 유지해 **미추적 상태**로 보존했다. 커밋에는 코드·테스트·집계 문서만 포함하며, 다른 checkout에서는 로컬 근거 링크가 존재하지 않는다. 패턴 적중 0건이 모든 민감 정보 부재를 보장하지는 않는다.

## 로컬 검증

- 시스템/개발자 지침 실제 전달, 핵심 훅 중복 지침 제거, 검색어 거부/정상 통과, 디렉터리 제거 근거, 승인 핵심 바이트 변경을 각 공개 경계에서 RED → GREEN으로 확인했다.
- Python unittest 29개 통과(T1 8, 저장 12, 기존/추가 T4 9). 외부 CLI stub 테스트는 인자/설정 전달만 증명하며 모델 행동 성공의 근거가 아니다.
- 기존 hooks 테스트 두 그룹 PASS, 임시 memory_mcp selftest OK, fixture 구조 PASS 11개, git diff --check 통과.
- 구조 검증은 행동/실제 회상 연결 성공이 아니다.

## 워크트리 상태

기존 Orca T3 워크트리 `/Users/max/orca/workspaces/95_personal-memory/memory-retrieval-t3`의 `64764d1`은 현재 HEAD의 조상이다. 미추적 T4 파일 6개가 남아 있고 현재 경로에 모두 후속 사본이 있다. 4개는 동일, 실행기/평가 테스트 2개는 현재 쪽에 후속 수정이 있다. Orca 상태는 not_running/stale_bootstrap이며 조회가 runtime_unavailable로 실패했다. 삭제·강제 제거·터미널 종료는 수행하지 않았다. 평가 완료의 선행 조건으로 워크트리 정리는 필요하지 않다.
